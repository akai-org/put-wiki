import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card';
export default function TempComp() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Prowadzone przedmioty</CardTitle>
      </CardHeader>
      <CardContent>Temp content</CardContent>
    </Card>
  );
}
