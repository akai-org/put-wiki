name: CD

on: workflow_dispatch

permissions:
  contents: write
  packages: write

jobs:
  detect-changes:
    runs-on: ubuntu-latest

    outputs:
      client: ${{ steps.filter.outputs.client }}
      server: ${{ steps.filter.outputs.server }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            client:
              - 'client/**'
            server:
              - 'server/**'

  set-version:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.client == 'true' || needs.detect-changes.outputs.server == 'true'
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create tag
        uses: anothrNick/github-tag-action@v1
        env:
          tag_prefix: v
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-client:
    runs-on: ubuntu-latest

    needs: 
      - detect-changes
      - set-version

    if: needs.detect-changes.outputs.client == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: setup QEMU
        uses: docker/setup-qemu-action@v4.0.0
      
      - name: setup Docker Buildx
        uses: docker/setup-buildx-action@v4.0.0

      - name: Build and push client image
        uses: docker/build-push-action@v7.2.0
        with:
          context: ./client
          push: true
          tags: ghcr.io/${{ github.repository }}/client:${{ needs.set-version.outputs.tag }}

  build-server:
    runs-on: ubuntu-latest

    needs: 
      - detect-changes
      - set-version

    if: needs.detect-changes.outputs.server == 'true'  

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Login to GHCR
        uses: docker/login-action@v4.1.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: setup QEMU
        uses: docker/setup-qemu-action@ce360397dd3f832beb865e1373c09c0e9f86d70a
      
      - name: setup Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd

      - name : Build and push server image
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf
        with:
          context: ./server
          push: true
          tags: ghcr.io/${{ github.repository }}/server:${{ needs.set-version.outputs.tag }}

  release:
    runs-on: ubuntu-latest

    needs:
      - detect-changes
      - set-version
      - build-client
      - build-server

    if: always() && (needs.detect-changes.outputs.client == 'true' || needs.detect-changes.outputs.server == 'true')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.set-version.outputs.tag }}
          name: Release ${{ needs.set-version.outputs.tag }}
          body: |
            Release for commit ${{ github.sha }}.
