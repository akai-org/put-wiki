services:
  maintenance:
    image: nginx:1.29.5-alpine
    container_name: maintenance_proxy
    ports:
      - "80:80"
    volumes:
      - ./maintenance/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./maintenance/index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
